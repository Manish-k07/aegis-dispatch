# AEGIS Computer-Aided Dispatch (CAD) System Configuration
# Mapbox / CARTO API key for high-resolution tile layers (Optional; falls back to free OSM clean tiles)
VITE_MAP_API_KEY=
VITE_CARTO_API_KEY=

# Backend API Base (defaults to http://localhost:8080/api)
VITE_API_BASE=http://localhost:8080/api
